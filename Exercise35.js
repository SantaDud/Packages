const animals = ["zebra", "horse", "donkey"];

for (let i = 0; i < animals.length; i++)
    console.log(`${pizzas[i]}`);

for (let i = 0; i < animals.length; i++)
    if (i === 0)
        console.log(`${pizzas[i]} have black and white stripes`);
    else if (i === 1)
        console.log(`${pizzas[i]} run very fast`);
    else if (i === 2)
        console.log(`${pizzas[i]} are still used to transport loads`);

console.log(`All of these animals belong to the same family called Equidae.`);